{
  "vendorName": "contacts",
  "script": {
    "body": "transformedObject.foo = 'bar'; done(transformedObject);"
  },
  "configuration": [{
    "type": "scriptEngine",
    "properties": {
      "value": "v2"
    }
  }],
  "fields": [{
    "path": "id",
    "vendorPath": "id"
  }]
}
