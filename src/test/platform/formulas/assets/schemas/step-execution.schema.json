{
  "type": "object",
  "properties": {
    "id": {
      "type": "integer"
    },
    "status": {
      "type": "string",
      "enum": [
        "failed",
        "success",
        "pending",
        "queued",
        "unknown"
      ]
    },
    "createdDate": {
      "type": "string"
    },
    "stepName": {
      "type": "string"
    }
  },
  "required": ["id", "status", "createdDate", "stepName"]
}
